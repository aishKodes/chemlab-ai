# Chemistry Content

This directory is reserved for future markdown or MDX lesson content.

The MVP currently uses structured data in `data/chemistry-modules.ts`, while
Supabase tables in `supabase/schema.sql` define the long-term content model.
