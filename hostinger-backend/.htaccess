Options -Indexes

<FilesMatch "^(\.env|composer\.(json|lock)|README\.md)$">
  Require all denied
</FilesMatch>

<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteRule ^(src|migrations|seeders|storage|vendor)(/.*)?$ - [F,L]
</IfModule>
